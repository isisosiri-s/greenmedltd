# Claude Code local settings
.claude/settings.local.json

# Excel temp files
~$*

# Screenshots (temporary)
temporary screenshots/

# Node modules (if ever added)
node_modules/

# Excel working file (not needed in repo)
products/Morfology Lab Product List Moby.xlsx

# SMTP credentials — real secret, uploaded to hosting manually via FTP only
smtp-config.php
